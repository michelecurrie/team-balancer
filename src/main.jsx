import React from 'react'
import ReactDOM from 'react-dom/client'
import TeamBalancer from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TeamBalancer />
  </React.StrictMode>,
) 
